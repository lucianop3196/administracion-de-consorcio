import * as React from "react";
import Layout from "../components/layout";
// markup
const AboutPage = () => {
  return (
    <Layout pageTitle="Sobre Nosotros">
      <p>
        Esta sección es para contar sobre nosotros y el servicio que brindamos.
      </p>
    </Layout>
  );
};

export default AboutPage;
